## cdp-shell scripts

Dev-only diagnostic scripts for inspecting and debugging the CDP shell against a running Chrome with `--remote-debugging-port`. These are not part of the published CLI surface and may be deleted, renamed, or break at any time. Run them ad-hoc with `node scripts/<name>.js [port]` from the `cdp-shell/` directory.
